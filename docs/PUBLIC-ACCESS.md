# Public access

The owner has enabled public access to Daymark. Each person's plans still require
an email-verified account and remain protected by per-user database access rules.

The selected custom domain is **daymarks.click**. Registration is in progress,
and DNS ownership and certificate validation must finish before it is live.
The previous custom-domain attachments have been removed from hosting.

See DOMAIN.md for activation instructions. Email delivery still needs a valid
sender credential and a real registration test. No DNS records were changed here.
